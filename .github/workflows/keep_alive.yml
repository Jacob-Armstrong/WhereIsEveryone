name: Keep Supabase Alive

on:
  schedule:
    # Run at midnight (0 0 * *) on Sunday (0) and Friday(5)
    - cron: '0 0 * * 0,5'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Database
        run: |
          curl -X GET "${{ secrets.APIURL }}/rest/v1/locations?select=id&limit=1" \
          -H "apikey: ${{ secrets.APIKEY }}" \
          -H "Authorization: Bearer ${{ secrets.APIKEY }}"
